# 🚀 完整設定指南
## 地政X經紀同根生 | Next.js + Supabase + LemonSqueezy

---

## 第一步：Supabase 設定（約10分鐘）

### 1-1 建立新專案
1. 進入 https://supabase.com → 點「New Project」
2. Organization 選「vekanji-ship-it's Org」（你剛建的）
3. Project name：`law-system`
4. Database Password：設一個強密碼（記下來）
5. Region：選「Northeast Asia (Tokyo)」（最近台灣）
6. 點「Create new project」，等待約2分鐘

### 1-2 取得 API 金鑰
1. 左側選單 → Project Settings → API
2. 複製以下三個值：
   - `Project URL`（格式：https://xxxx.supabase.co）
   - `anon public`（public API key）
   - `service_role`（⚠️ 這個要保密，不可公開）

### 1-3 建立資料表
1. 左側選單 → SQL Editor
2. 點「New query」
3. 貼上 `supabase-setup.sql` 的所有內容
4. 點「Run」→ 看到 Success 即完成

### 1-4 設定 Email 驗證（讓用戶可以註冊）
1. 左側選單 → Authentication → Providers
2. 確認「Email」已啟用
3. Authentication → URL Configuration：
   - Site URL：填你的網址（之後部署後改）
   - Redirect URLs：加入 `https://你的網址.vercel.app/dashboard`

---

## 第二步：LemonSqueezy 設定（約15分鐘）

### 2-1 完成帳號設定
1. 輸入 2FA 驗證碼完成登入
2. 填寫基本資料（Name, Country 選 Taiwan）

### 2-2 建立 Store
1. 左側「Stores」→「Create Store」
2. Store name：`Kmoji` 或 `地政同根生`
3. Store URL：設定你想要的網址後綴
4. Currency：TWD（台幣）

### 2-3 建立產品
1. 左側「Products」→「Create product」

**月費版：**
- Name：`地政X經紀同根生 月費版`
- Price：NT$199（或你想要的價格）
- Billing：Recurring（訂閱制）→ Monthly
- 點「Save product」

**年費版：**（可選）
- Name：`地政X經紀同根生 年費版`
- Price：NT$1,990
- Billing：Recurring → Yearly

### 2-4 取得付款連結
1. 點進你的產品
2. 找到「Checkout URL」或「Share」
3. 複製這個連結，貼到程式碼中（搜尋 `your-lemonsqueezy-link.com` 替換）

### 2-5 設定 Webhook（付費後自動產生授權碼）
1. 左側「Settings」→「Webhooks」
2. 點「Add webhook」
3. URL：`https://你的網址.vercel.app/api/webhooks/lemonsqueezy`
4. Events：勾選 `order_created`、`subscription_created`
5. Signing secret：設一個亂數字串（記下來，填入環境變數 LEMONSQUEEZY_WEBHOOK_SECRET）

### 2-6 取得 API Key
1. Settings → API → 點「Create new API key」
2. 複製並記下（只顯示一次）

---

## 第三步：部署到 Vercel（約5分鐘）

### 3-1 上傳程式碼到 GitHub
1. 打開你的 GitHub（vekanji-ship-it/law-system）
2. 把所有程式碼檔案上傳（可直接拖拉或用 GitHub Desktop）
3. **重要**：`.env.local` 不要上傳！

### 3-2 在 Vercel 設定環境變數
1. 進入 https://vercel.com → 你的 law-system 專案
2. Settings → Environment Variables
3. 逐一新增以下變數：

| 變數名稱 | 值 |
|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | https://你的ID.supabase.co |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | anon key（從Supabase複製）|
| `SUPABASE_SERVICE_ROLE_KEY` | service role key（保密！）|
| `LEMONSQUEEZY_API_KEY` | LemonSqueezy API Key |
| `LEMONSQUEEZY_WEBHOOK_SECRET` | 你設的 Webhook Secret |
| `NEXT_PUBLIC_SITE_URL` | https://你的網址.vercel.app |

4. 點「Save」後，重新部署（Deployments → Redeploy）

---

## 第四步：測試流程

### 4-1 測試註冊/登入
1. 開啟你的網址
2. 點「免費註冊」→ 填email和密碼
3. 去信箱點確認連結
4. 成功登入 → 進入 Dashboard

### 4-2 手動測試授權碼
1. 進入 Supabase → Table Editor → licenses
2. 點「Insert row」手動新增：
   - license_key：`KM-TEST-ABCD-1234`
   - plan：`monthly`
   - is_active：`true`
   - expires_at：2027-01-01
3. 回到網站 → `/activate`
4. 輸入 `KM-TEST-ABCD-1234` 測試

### 4-3 測試付款流程（LemonSqueezy sandbox）
1. LemonSqueezy 有測試模式，先用測試信用卡完成付款
2. 確認 Webhook 有收到 → 確認 licenses 資料表有新增一筆
3. 確認授權碼寄到信箱（需要設定寄信功能）

---

## 第五步：設定寄信（讓用戶收到授權碼）

Supabase 內建 Email 寄送。在 Webhook 處理完後，可用以下方式寄信：

**方案A：用 Supabase 內建 Email（簡單）**
在 webhook route.js 的成功區塊加入：
```
await supabase.auth.admin.inviteUserByEmail(email, {
  data: { license_key: licenseKey }
})
```

**方案B：用 Resend（更專業，有美觀模板）**
- 申請 https://resend.com（免費方案100封/天）
- 安裝：`npm install resend`
- 在 webhook 中加入寄信邏輯

---

## 常見問題

**Q：用戶付費後沒收到授權碼？**
A：檢查 Vercel Logs → Functions → 看 webhook 是否有錯誤

**Q：登入後一直跳回登入頁？**
A：確認 Supabase 的 Site URL 設定是否正確

**Q：Vercel 部署失敗？**
A：確認所有環境變數都已填寫

---

*設定完成後，你就有一個完整的：登入系統 + 授權碼驗證 + 付費解鎖 的地政知識平台！*
